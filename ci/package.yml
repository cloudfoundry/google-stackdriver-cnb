---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: golang
    tag: latest

inputs:
- name: google-stackdriver-buildpack

outputs:
- name: artifactory

caches:
- path: go-module-cache

run:
  path: google-stackdriver-buildpack/ci/package.sh
